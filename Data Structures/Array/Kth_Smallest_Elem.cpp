#include <bits/stdc++.h>
using namespace std;

int kthSmallestElem(int arr[], int n, int k)
{
    
}

int main()
{
    int arr[6] = {7, 10, 4, 3, 20, 15};
    int n = sizeof(arr) / sizeof(arr[0]);
    int k = 3;
    kthSmallestElem(arr, n - 1, k);
    return 0;
}